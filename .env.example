# Backend (Node) environment variables
# Copy to .env (DO NOT commit .env)

# Server
PORT=3001
# For production, set this to your Vercel domain(s)
# Comma-separated is supported: https://a.com,https://b.com
CORS_ORIGIN=http://localhost:5173

# Admin (used for /game/<ADMIN_SECRET>)
ADMIN_SECRET=wwrt0gjfe80r8hr

# Alwaysdata MySQL
DB_HOST=mysql-youraccount.alwaysdata.net
DB_PORT=3306
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=your_db_name

# Aliases also supported by the server:
# DB_USERNAME (for DB_USER)
# DB_PASSWORD (for DB_PASS)
# DB_DATABASE (for DB_NAME)
