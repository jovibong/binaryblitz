# Frontend (Vite/React) environment variables
# Copy to client/.env (DO NOT commit client/.env)

# Where the browser should connect for Socket.IO and /health.
# Local dev default is http://localhost:3001, so this is optional locally.
VITE_SERVER_URL=http://localhost:3001
